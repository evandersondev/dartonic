## 1.0.0

First stable release. SQLite driver for the modular Dartonic ecosystem —
`connectSqlite()` opens a type-safe connection backed by `sqlite3`. Pair with
[`dartonic_core`](https://pub.dev/packages/dartonic_core).
