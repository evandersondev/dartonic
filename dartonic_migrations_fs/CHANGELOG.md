## 1.0.0-beta.1

Initial beta of the filesystem migration loader for Dartonic — reads `.sql`
files from a directory in CLI/server environments (not for Flutter web). The
API may change before a stable 1.0.0.
