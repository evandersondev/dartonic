name: dartonic_migrations_fs
description: Filesystem migration loader for Dartonic. Reads .sql files from a directory in CLI/server environments. Not for Flutter web.
version: 1.0.0-beta.1
homepage: https://github.com/evandersondev/dartonic
repository: https://github.com/evandersondev/dartonic
topics:
  - orm
  - database
  - migrations

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dartonic_core: ^1.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
