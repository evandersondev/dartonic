name: dartonic_core
description: Core query builder, schema DSL, and interfaces for Dartonic. No database driver dependencies.
version: 1.0.0
homepage: https://github.com/evandersondev/dartonic

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  uuid: ^4.5.1
  path: ^1.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
